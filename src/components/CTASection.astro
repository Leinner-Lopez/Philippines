---
---

<section id="briefing" class="cta">
	<div class="container">
		<div class="cta-card">
			<p class="eyebrow"><span class="dot"></span> Editorial Country Profile</p>
			<h2>Six pulses, one archipelago on the move.</h2>
			<p class="description">
				From a fast-growing economy to a rising digital scene, a resilient culture, an
				expanding healthcare system, a young bilingual workforce, and a celebrated cuisine — the
				Philippines keeps writing its next chapter.
			</p>
			<a class="link" href="#top">Back to overview →</a>
		</div>
	</div>
</section>

<style>
	.cta-card {
		background: var(--gradient-cta);
		border-radius: var(--radius-lg);
		padding: clamp(2rem, 5vw, 3.5rem);
		color: white;
		box-shadow: var(--shadow-soft);
	}

	.eyebrow {
		color: rgba(255, 255, 255, 0.85);
	}

	.eyebrow .dot {
		width: 6px;
		height: 6px;
		border-radius: 50%;
		background: white;
	}

	h2 {
		margin: 0.6rem 0 0.9rem;
		font-size: clamp(1.7rem, 3.4vw, 2.4rem);
		font-weight: 800;
		max-width: 24ch;
	}

	.description {
		max-width: 56ch;
		color: rgba(255, 255, 255, 0.88);
		margin: 0 0 1.5rem;
	}

	.link {
		display: inline-flex;
		align-items: center;
		gap: 0.4rem;
		font-weight: 700;
		text-decoration: none;
		color: white;
		background: rgba(255, 255, 255, 0.16);
		padding: 0.75rem 1.3rem;
		border-radius: var(--radius-pill);
	}
</style>
